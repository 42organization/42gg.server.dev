spring:
  config:
    activate:
      on-profile:

  # 데이터 소스 설정
  datasource:
    url: jdbc:mysql://localhost:3306/TEST?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driverClassName: com.mysql.cj.jdbc.Driver
    username: 42gg
    password: pingPongpower42#

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: false
        use_sql_comments: false


  security:
    oauth2.client:
      registration:
        42:
          client-id: "3111893d1b711fe2b6defa524f9a138a7048e2a010079785fa5658c965a4fc75"
          client-secret: "759ac1501d6c08b0124ce04f6b92960d5e79320bd9f44ebd4f288e6b3c05cbcc"

  # Redis 설정
  cache:
    type: redis
  redis:
    host: 127.0.0.1
    port: 6379


logging:
  level:
    org.hibernate.SQL: debug

  # cors 설정
cors:
  allowed-origins: 'http://localhost:3000'
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
  allowed-headers: '*'
  allowed-credentials: false
  max-age: 3600

info:
  web:
    frontUrl: 'http://localhost:3000'
